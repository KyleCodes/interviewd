{
  "name": "gateway",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t interviewd-gateway:${TAG:-dev} apps/gateway",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
