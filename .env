# Tag pointers (deploy/rollback = repoint + `docker compose up -d <service>`).
API_TAG=dev
WORKER_TAG=dev
GATEWAY_TAG=dev
AUTOSCALER_TAG=dev

# Host ports
GATEWAY_PORT=8080
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090
PG_PORT=5433

# Saturation dial: artificial per-job cost in the worker.
WORK_MS=300
TRAFFIC_TAG=dev
TRAFFIC_PORT=8090
