{
  "name": "api",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t interviewd-api:${TAG:-dev} apps/api",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
